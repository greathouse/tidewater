apply from: "$projectDir/../gradle/java.gradle"
apply from: "$projectDir/../gradle/versions.gradle"

dependencies {
    compile peerProject('core') //project("${project.parent.path}core")

    testCompile groovy
    testCompile groovySql
    testCompile h2
    testCompile testng
}