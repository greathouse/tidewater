apply from: "$projectDir/../plugin.gradle"

repositories {
    maven {
        url 'https://oss.sonatype.org/content/groups/public/'
    }
}

dependencies {
    plugin ('com.github.docker-java:docker-java:3.0.0-SNAPSHOT') {
        exclude group: 'org.slf4j'
    }
}